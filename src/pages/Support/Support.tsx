import { Typography } from '@mui/material';

export const Support = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-[#2c2d55] px-4 text-white">
      <Typography variant="h6">Work in Progress...</Typography>
    </main>
  );
};
